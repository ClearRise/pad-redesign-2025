{%- liquid
  unless limit
    assign limit = main_menu.links.size
  endunless
  unless offset
    assign offset = 0
  endunless
-%}

{{ 'header-desktop-nav.css' | asset_url | stylesheet_tag }}

<ul
  class="site-nav site-navigation small--hide"
  {% unless disable_aria %}
    role="navigation" aria-label="Primary"
  {% endunless %}>
  {%- for link in main_menu.links limit: limit offset: offset -%}
    {%- liquid
      assign has_dropdown = false
      assign is_megamenu = false
      if link.links != blank
        assign has_dropdown = true
        if link.levels > 1
          assign is_megamenu = true
        endif
      endif
    -%}

    <li
      class="site-nav__item site-nav__expanded-item{% if has_dropdown %} site-nav--has-dropdown{% endif %}{% if is_megamenu %} site-nav--is-megamenu{% endif %}"
      {% if has_dropdown %}aria-haspopup="true"{% endif %}>

      <a href="{{ link.url }}" class="site-nav__link site-nav__link--underline{% if has_dropdown %} site-nav__link--has-dropdown{% endif %}">
        {{ link.title }} 
      {%- if is_megamenu or has_dropdown -%}
        <span class="faux-button">
           {%- render 'collapsible-icons' -%}
        </span>
      {%- endif -%}  
      </a>
      {%- if is_megamenu -%}
        {%- assign previous_column_type = '' -%}
        {%- assign animation_column = 1 -%}

        <div class="site-nav__dropdown megamenu text-left">
          <div class="page-width">
            <div class="grid{% if dropdown_alignment == 'center' %} grid--center{% endif %}">
              <div class="grid__item medium-up--one-fifth appear-animation appear-delay-{{ animation_column }}">
                {%- assign animation_column = animation_column | plus: 1 -%}

                {%- for childlink in link.links -%}
                  {%- liquid
                    assign create_new_column = false

                    if childlink.levels > 0 and forloop.index != 1
                      assign create_new_column = true
                    endif

                    if childlink.levels == 0 and previous_column_type == 'full'
                      assign create_new_column = true
                    endif
                  -%}

                  {%- if create_new_column -%}
                    </div><div class="grid__item medium-up--one-fifth appear-animation custom_column appear-delay-{{ animation_column }}">
                    {%- assign animation_column = animation_column | plus: 1 -%}
                  {%- endif -%}

                  {%- assign subcollection_drop = false -%}
                  {%- if childlink.levels > 0 and childlink.url contains '/collections/' -%}
                    {%- liquid
                      assign lang_code_string = request.locale.iso_code | prepend: '/' | downcase
                      assign subcollection_handle = childlink.url | remove: '/collections/' | remove: lang_code_string
                      assign subcollection_drop = collections[subcollection_handle]
                    -%}
                    {%- if subcollection_drop.image != blank -%}
                      <a
                        href="{{ childlink.url }}"
                        class="megamenu__colection-image"
                        aria-label="{{collections[subcollection_handle].title}}"
                        style="background-image: url({{ collections[subcollection_handle].image | img_url: '400x' }})"></a>
                    {%- endif -%}
                  {%- endif -%}

                  <div class="h5">
                    <a href="{{ childlink.url }}" class="site-nav__dropdown-link site-nav__dropdown-link--top-level">{{ childlink.title }}</a>
                  </div>

                  {%- liquid
                    if childlink.levels > 0
                      assign previous_column_type = 'full'
                    else
                      assign previous_column_type = 'single'
                    endif
                  -%}
                 
                <div class="menu_item_custom {{ childlink.title | downcase | strip | replace: ' ', '-' }}">
                  
                  {%- for grandchildlink in childlink.links -%}
 
                          {%- assign subsubcollection_drop = false -%}
                          {%- if grandchildlink.url contains '/collections/' -%}
                            <div class="menu_item_custom_inner">
                          {%- liquid
                            assign lang_code_string = request.locale.iso_code | prepend: '/' | downcase
                            assign subsubcollection_handle = grandchildlink.url | remove: '/collections/' | remove: lang_code_string
                            assign subsubcollection_drop = collections[subsubcollection_handle]
                          -%}
                             <a href="{{ grandchildlink.url }}" class="site-nav__dropdown-link">
                          {%- if subsubcollection_drop.image != blank -%}
                            <span
                              class="icon-swatch megamenu__colection-image"
                              aria-label="{{collections[subsubcollection_handle].title}}"
                              style="background-image: url({{ collections[subsubcollection_handle].image | img_url: '40x' }})"></span>
                          {%- endif -%}
                            {{grandchildlink.title}}
                             </a>
                            </div>
                         {% else %}  
                             <a href="{{ grandchildlink.url }}" class="site-nav__dropdown-link">
                              {{grandchildlink.title}}
                             </a>
                        {%- endif -%}
                  {%- endfor -%}
                  </div>
                {%- endfor -%}
                
              </div>
            </div>
          </div>
        </div>
      {%- elsif has_dropdown -%}
        <ul class="site-nav__dropdown text-left">
          <div class="li-container">
            <h6>{{ link.title }}</h6>
            {%- for childlink in link.links -%}
              {%- liquid
                assign has_sub_dropdown = false
                if childlink.links != blank
                  assign has_sub_dropdown = true
                endif
              -%}

              <li class="{% if has_sub_dropdown %} site-nav__deep-dropdown-trigger{% endif %}">
                {%- assign subcollection_drop = false -%}
        
                <a href="{{ childlink.url }}" class="site-nav__dropdown-link site-nav__dropdown-link--second-level{% if has_sub_dropdown %} site-nav__dropdown-link--has-children{% endif %}">
                  {{ childlink.title | escape }}
                  {%- if has_sub_dropdown -%}
                    <svg aria-hidden="true" focusable="false" role="presentation" class="icon icon--wide icon-chevron-down" viewBox="0 0 28 16"><path d="M1.57 1.59l12.76 12.77L27.1 1.59" stroke-width="2" stroke="#000" fill="none" fill-rule="evenodd"/></svg>
                  {%- endif -%}
                </a>
                {%- if has_sub_dropdown -%}
                  <ul class="site-nav__deep-dropdown">
                    {%- for grandchildlink in childlink.links -%}
                      <li>
                        <a href="{{ grandchildlink.url }}" class="site-nav__dropdown-link">{{ grandchildlink.title | escape }}</a>
                      </li>
                    {%- endfor -%}
                  </ul>
                {%- endif -%}
              </li>
            {%- endfor -%}
          </div>
          <div class="featured_collection-container">
            {% for featured_collection in blocks %}
              {% if featured_collection.type == "featured_collection" %}
                {% assign menu_label = featured_collection.settings.menu_lable | downcase | escape %}
                {% assign link_title = link.title | downcase | escape %}
                {% if menu_label == link_title %}
                  <a class="featured-collection-container" href="/collections/{{ featured_collection.settings.url }}">
                    {% assign featured_collection_selected = collections[featured_collection.settings.collection] %}
                    {% if featured_collection_selected.featured_image or featured_collection.settings.featured_image %}
                      {% if featured_collection.settings.featured_image != blank %}
                        <img src="{{ featured_collection.settings.featured_image | img_url: 'large' }}" alt="{{ featured_collection_selected.title }}">
                      {% else %}
                        <img src="{{ featured_collection_selected.featured_image | img_url: 'large' }}" alt="{{ featured_collection_selected.title }}">
                      {% endif %}
                      <span>Shop by</span>
                      <h6>{{ featured_collection_selected.title }}</h6>
                    {% endif %}
                  </a>
                {% endif %}
              {% endif %}
            {% endfor %}
          </div>
        </ul>
      {%- endif -%}
    </li>
  {%- endfor -%}
</ul>
